Simply extract this folder and import the Eclipse project in Eclipse. It might be, that the external libraries will need to be added again. 
Simply right click on the project and click on Build Path->configure build path. In the picture build-path.png the imported libraries are shown. 
The libraries in the folder libs need to be added.
